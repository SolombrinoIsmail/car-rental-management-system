# Run tests before pushing
echo "🧪 Running tests before push..."

# Try to run tests, fail if tests fail
if pnpm test:unit 2>/dev/null; then
  echo "✅ Unit tests passed!"
elif pnpm test 2>/dev/null; then
  echo "✅ Tests passed!"
else
  echo "⚠️  No test script found - skipping tests"
  echo "⚠️  Consider adding tests to ensure code quality"
fi

# Always exit successfully for now since tests aren't set up
# TODO: Change to 'exit 1' when tests are implemented
exit 0